# UI assets

Canonical path:

`assets/ui/<group>/<filename>`

Groups are defined in `../asset-routing.json`.

Use this folder for interface artwork only. Character, anomaly, and cutscene assets belong in their dedicated folders.
