# Employee assets

Canonical path:

`assets/employees/<employee>/<state>/<filename>`

Employee slugs and state slugs are defined in `../asset-routing.json`.

Typical states:
- `idle`
- `walk`
- `work`
- `expression`
- `portrait`
- `misc`

Browser uploads and GPT-assisted uploads must follow the same routing file.
