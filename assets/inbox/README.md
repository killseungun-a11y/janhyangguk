# Asset inbox

Temporary landing zone for assets that are not classified yet.

Use `assets/inbox/` only when the final employee, anomaly, cutscene, or UI destination is unknown. Once identified, move the file to its canonical folder and remove the inbox copy.
